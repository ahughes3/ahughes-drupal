{#
/**
 * @file
 * Default theme implementation to present a media item.
 *
 * Available variables:
 * - media: The media item, with limited access to object properties and
 *   methods. Only method names starting with "get", "has", or "is" and
 *   a few common methods such as "id", "label", and "bundle" are available.
 *   For example:
 *   - entity.getEntityTypeId() will return the entity type ID.
 *   - entity.hasField('field_example') returns TRUE if the entity includes
 *     field_example. (This does not indicate the presence of a value in this
 *     field.)
 *   Calling other methods, such as entity.delete(), will result in
 *   an exception.
 *   See \Drupal\Core\Entity\EntityInterface for a full list of methods.
 * - name: Name of the media item.
 * - content: Media content.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 *
 * @see template_preprocess_media()
 *
 * @ingroup themeable
 */
#}

{% set video_attributes = create_attribute() %}

{% set video_autoplay = media.field_autoplay_video.value ? true : false %}
{% set video_controls = media.field_show_controls.value ? true : false %}
{% set video_muted = media.field_mute_video.value ? true : false %}
{% set video_loop = media.field_automatically_loop_video.value ? true : false %}
{% set video_poster = media.field_poster_image.entity.uri.value ?  media.field_poster_image.entity.uri.value|image_style('max_1300x1300') %}
{% set video_url = file_url(media.field_media_video_file.entity.uri.value) %}
{% set video_caption = media.field_file_video_caption_text.value %}

{% set video_attributes = video_attributes
    .setAttribute('autoplay', video_autoplay)
    .setAttribute('controls', video_controls)
    .setAttribute('muted', video_muted)
    .setAttribute('loop', video_loop)
    .setAttribute('poster', video_poster)%}

{% include "@atoms/video/video.twig" %}
